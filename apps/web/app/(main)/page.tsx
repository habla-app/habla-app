// Landing pública — vista simplificada previa al Sub-Sprint 3 que construye
// `/matches` con layout desktop completo. En Phase 2 solo migramos tokens:
// hero live (dark-card) + tabs + cards de torneos con paleta nueva.
import { HomeContent } from "@/components/home/HomeContent";

export default function HomePage() {
  return <HomeContent />;
}
