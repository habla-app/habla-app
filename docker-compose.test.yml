services:
  postgres-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: habla_test
      POSTGRES_PASSWORD: habla_test
      POSTGRES_DB: habladb_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data

  redis-test:
    image: redis:7-alpine
    ports:
      - "6380:6379"
    tmpfs:
      - /data
